# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase browser configuration
NEXT_PUBLIC_SUPABASE_URL=https://jqqzemvyvjujvpgvongw.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only. Required for administrator user invitations.
SUPABASE_SERVICE_ROLE_KEY=

# PostgreSQL server configuration
# Use the transaction pooler for deployed serverless runtime connections.
DATABASE_URL=
# Use the session pooler for migrations when direct IPv6 is unavailable.
DIRECT_URL=

# Zoho OAuth and regional API base
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REFRESH_TOKEN=
ZOHO_ACCOUNTS_URL=https://accounts.zoho.eu
ZOHO_API_URL=https://www.zohoapis.eu
ZOHO_CRM_API_URL=https://www.zohoapis.eu/crm/v8
ZOHO_MAIL_API_URL=https://mail.zoho.eu/api
ZOHO_MAIL_ACCOUNT_ID=

# WorkDrive
ZOHO_WORKDRIVE_TEAM_ID=
ZOHO_WORKDRIVE_ROOT_FOLDER_ID=

# Report email delivery. Use "zoho_mail" or "resend".
EMAIL_PROVIDER=zoho_mail
REPORT_EMAIL_FROM=
RESEND_API_KEY=

# Never commit database passwords, connection strings, secret/service-role keys,
# or Zoho OAuth credentials. Put them in .env.local and deployment secrets.
